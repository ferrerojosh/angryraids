// https://github.com/milligram/milligram-stylus/blob/master/src/_Form.styl

input
  -webkit-appearance: none !important

input[type='email'],
input[type='number'],
input[type='password'],
input[type='search'],
input[type='tel'],
input[type='text'],
input[type='url'],
textarea,
select
  color: color-secondary
  appearance: none // Removes awkward default styles on some inputs for iOS
  background-color: transparent
  border: .1rem solid color-border
  border-radius: 0 !important // Enforce a non rounded border, because S A F A R I
  -webkit-border-radius: 0 !important
  box-shadow: none
  box-sizing: inherit // Forced to replace inherit values of the normalize.css
  height: 3.8rem
  padding: .6rem 1.0rem // The .6rem vertically centers text on FF, ignored by Webkit
  width: 100%

  &:focus
    border-color: color-primary
    outline: 0
